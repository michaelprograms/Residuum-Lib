
pointerp(3)           LPC Library Functions           pointerp(3)

NAME
       pointerp()  - determine whether or not a given variable is
       an array

SYNOPSIS
       int pointerp( mixed arg );

DESCRIPTION
       Return 1 if `arg' is an array.

SEE ALSO
       mapp(3),  stringp(3),  objectp(3),  intp(3),   bufferp(3),
       floatp(3),    functionp(3),    nullp(3),    undefinedp(3),
       errorp(3)

MudOS                       5 Sep 1994                          1

